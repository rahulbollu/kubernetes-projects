# adservice/Dockerfile
FROM golang:1.21-alpine AS builder
WORKDIR /app
COPY . .
RUN go build -o adservice .

FROM alpine:latest
WORKDIR /app
COPY --from=builder /app/adservice .
EXPOSE 8080
ENTRYPOINT ["./adservice"]
